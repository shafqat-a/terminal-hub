[toolchain]
# Plan originally pinned 1.79, but transitive deps of reqwest/tokio-tungstenite
# (icu_*, idna_adapter, hyper-rustls 0.27+) require rustc >= 1.86.
channel = "1.86"
components = ["rustfmt", "clippy"]
